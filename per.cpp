#include "per.h"

per::per(const Hand& my, const Hand& pc)
{

    cout << endl;
    cout << line;
    cout << endl;
    /*
    cout << "PC Serial Deck" << endl;
    cout << endl;
    vector<Deck> pcSerialDecks = pc.getSerialAlignedHand();
    for (int i = 0; i < pcSerialDecks.size(); ++i)
    {
        Deck deck = pcSerialDecks.at(i);
        deck.printDeck();
        cout << "     ";
    }
    */
}
